// import { calc_weighted_routes, Connection } from 'eve-routes';



// const fetchRoutes = async (originId: number, destinations: number[], connections: Connection[], flag: string) => {
//   const data = {};
//   const ends = new Uint32Array(destinations);

//   // for (let i = 0; i < routes.length; i++) {
//   //   let route = routes[i];

//   //   data[destinations[i]] = {
//   //     name: details[destinations[i]].name,
//   //     route: route.map(id => details[id]),
//   //   };
//   // }

//   return await calc_weighted_routes(originId, ends, flag, connections);
// }